"use client";

// src/components/RainbowKitProvider/ShowBalanceContext.tsx
import React, {
  createContext,
  useContext,
  useState
} from "react";
var ShowBalanceContext = createContext({
  showBalance: void 0,
  setShowBalance: () => {
  }
});
function ShowBalanceProvider({ children }) {
  const [showBalance, setShowBalance] = useState();
  return /* @__PURE__ */ React.createElement(ShowBalanceContext.Provider, { value: { showBalance, setShowBalance } }, children);
}
var useShowBalance = () => useContext(ShowBalanceContext);

export {
  ShowBalanceProvider,
  useShowBalance
};
