import { type BaseAccountParameters } from 'wagmi/connectors';
import type { Wallet } from '../../Wallet';
export interface BaseOptions {
    appName: string;
    appIcon?: string;
}
type AcceptedBaseParameters = Omit<BaseAccountParameters, 'appName' | 'appLogoUrl'>;
interface Base extends AcceptedBaseParameters {
    (params: BaseOptions): Wallet;
}
export declare const base: Base;
/**
 * @deprecated Use `base` instead. This alias will be removed in a future version.
 */
export declare const baseAccount: Base;
export {};
